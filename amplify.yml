version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node -v
        - npm ci
    build:
      commands:
        - npm run build || true
        - node -e "try{require('fs').rmSync('build/types/components',{recursive:true,force:true});}catch(e){}"
  artifacts:
    # The production demo build outputs static files to `demo/`
    baseDirectory: demo
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
